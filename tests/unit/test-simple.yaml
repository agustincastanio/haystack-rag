suite: Test Simple Template
templates:
  - namespace.yaml

tests:
  - it: should render namespace
    template: namespace.yaml
    asserts:
      - hasDocuments:
          count: 1
      - contains:
          kind: Namespace
          metadata:
            name: haystack-rag 